<!-- Includes robot localization and tilt scanner launches for odometry sanity check-->
<launch>
    <!-- EKF for localization -->
    <!--node pkg="robot_localization" type="ekf_localization_node"
      name="ekf_localization">
      <rosparam command="load"
        file="$(find schroedi)/config/robot_localization.yaml" />
    </node-->

    <!-- Launch Tilt Scanner 3D including the SICK 2D scanner -->
    <include file = "$(find ohm_tilt_scanner_3d)/launch/tilt_scanner_3d_schroedi.launch"/>

     <node pkg="laser_scan_matcher" type="laser_scan_matcher_node"
      name="laser_scan_matcher_node" output="screen">

      <remap from="scan" to="/scan_republished"/>
      <remap from="odom" to="/odom"/>

      <param name="publish_pose_stamped" value="false"/>
      <param name="publish_pose" value="false"/>
      <param name="publish_tf" value="true"/>
      <param name="fixed_frame" value="/odom"/>
      <param name="base_frame" value="/base_footprint"/>

      <param name="use_imu" value="false"/>
      <param name="use_odom" value="false"/>
      <param name="use_vel" value="false"/>
    </node>
</launch>